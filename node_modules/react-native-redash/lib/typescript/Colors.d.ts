import Animated from "react-native-reanimated";
interface RGBColor {
    r: number;
    g: number;
    b: number;
}
declare const interpolateColors: (animationValue: number | Animated.Node<number>, inputRange: number[], colors: RGBColor[]) => any;
export default interpolateColors;
